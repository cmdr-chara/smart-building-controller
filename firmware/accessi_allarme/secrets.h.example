#pragma once

// Copy this file to secrets.h (ignored by Git) and fill the values below.
// Use the same 32+ character token configured as SMART_CONTROLLER_API_TOKEN
// on the PHP server.
const char* API_TOKEN = "";

// PEM-encoded root CA that validates SERVER_BASE_URL.
// Do not replace this with client.setInsecure().
const char* TLS_ROOT_CA = "";
