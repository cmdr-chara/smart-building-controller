name: Auto Release
on:
  push:
    paths: ["package.json", "pyproject.toml", "Cargo.toml", "Project.toml", "pubspec.yaml", "gradle.properties", "pom.xml", "*.csproj", "VERSION", "VERSION.txt", "version.txt"]
  workflow_dispatch:
permissions:
  contents: write
jobs:
  release:
    uses: cmdr-chara/codex-toolkit/.github/workflows/reusable-auto-release.yml@7ef6bedfd799491938a339536053e51641df1a18
